# Server
HOST=localhost
PORT=1337
NODE_ENV=development

# Secrets
APP_KEYS=changeme1,changeme2
API_TOKEN_SALT=changeme
ADMIN_JWT_SECRET=changeme
TRANSFER_TOKEN_SALT=changeme
ENCRYPTION_KEY=changeme
JWT_SECRET=changeme

# Database (SQLite par défaut pour dev)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Pour PostgreSQL en production :
# DATABASE_CLIENT=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=strapi
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi
# DATABASE_SSL=false
# Railway uses a self-signed cert chain, so rejectUnauthorized defaults to false.
# Set to true only if your provider supplies a fully trusted CA certificate.
# DATABASE_SSL_REJECT_UNAUTHORIZED=false

# Preview integration
CLIENT_URL=http://localhost:3000
PREVIEW_SECRET=changeme
ALLOWED_ORIGINS=http://localhost:3000,https://ton-site.com
# Example production value for Amanda Traduction:
# ALLOWED_ORIGINS=https://amandatraduction.com,https://www.amandatraduction.com,https://traduction-amanda-production.up.railway.app
# Note: previews on Vercel are automatically allowed by regex in the default middleware implementation; you don't have to enumerate them.

# Cloudinary (production) — RECOMMENDED: set a single CLOUDINARY_URL
# Ex: CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# OR provide the individual vars below (not recommended in prod):
# CLOUDINARY_NAME=your_cloud
# CLOUDINARY_KEY=your_key
# CLOUDINARY_SECRET=your_secret
